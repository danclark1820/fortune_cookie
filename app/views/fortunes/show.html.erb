<div class="fortune_cookie">
</div>
<div class="fortune">
  <%= @fortune.fortune %><br>
  -<%= @fortune.author%>
</div>
<div class="fortune_note">
  <%= simple_form_for [@fortune, @note] do |f| %>
    <%= f.input :body, required: :true %>
    <% if user_signed_in? %>
      <%= f.hidden_field :user_id, value: "#{current_user.id}" %>
    <%end%>
    <div id="tz_input"></div>
    <%= f.button :submit, "Submit" %>
  <% end %>
<div>
